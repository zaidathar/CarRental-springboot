#Generated by Eclipse Messages Editor (Eclipse Babel)

logging.level.org.hibernate.SQL  = DEBUG
logging.level.org.hibernate.type = TRACE

spring.datasource.password              = root
# DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url                   = jdbc:mysql://localhost:3306/cps?useSS\
                                          L=false&serverTimezone=UTC&useLegacyD\
                                          atetimeCode=false&allowPublicKeyRetri\
                                          eval=true
spring.datasource.username              = root
# Hibernate ddl auto (create, create-drop, validate, update)
#spring.jpa.hibernate.ddl-auto = create-drop
spring.jpa.hibernate.ddl-auto           = update
# Hibernate
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDia\
                                          lect
